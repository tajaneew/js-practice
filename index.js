let Choice = prompt('pick a number, any number');

let Choice2 = prompt('one more time')
  

if (Choice >= Choice2) {
    alert('the larger number between ' + Choice + ' and ' + Choice2 + ' is ' + Choice);
} else (Choice2>Choice); {
    alert('the larger number between ' + Choice + ' and ' + Choice2 + ' is ' + Choice2);
}
